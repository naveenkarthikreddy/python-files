# for stacks we used to use only arrays,
# we can use both arrays and linked list
# but due to time complexity we skip the
# linked list for stacks

# for quee we used to use only linked list,
# we can use both arrays and linked list
# but due to time complexity we skip the
# arrays for quees
